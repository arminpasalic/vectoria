# Dependencies
node_modules/

# Vercel local project metadata and build output
.vercel/

# Environment files and local secrets
.env
.env.*
!.env.example
!.env.*.example
*.local

# Local HTTPS certificates
server.key
server.crt
*.pem

# Logs and runtime files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.pid
*.pid.lock

# Test and tool output
coverage/
.nyc_output/
.cache/
.eslintcache

# Local SDK/toolchain installs
emsdk/

# OS files
.DS_Store
Thumbs.db

# Editors and local assistant settings
.vscode/
.idea/
.claude/
LLM_CLEARANCE.md
*.swp
*.swo
*~

# Temporary files
tmp/
temp/
*.tmp

# Keep deployment manifests, lockfiles, source, and the generated
# web_interface/static/mcp-server/ bundle committed. Vercel's `builds`
# configuration does not run buildCommand, so that bundle is deploy input.
